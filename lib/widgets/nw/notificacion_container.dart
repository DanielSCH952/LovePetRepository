import 'package:flutter/material.dart';

class NotificacionContainer extends StatelessWidget {
  const NotificacionContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}